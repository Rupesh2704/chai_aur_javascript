// For Loop 
let array = [1 , 2 , 3 ]

for (let index = 0; index < array.length; index++) {
    const element = array[index];
    
    if(element == 3 ){
        console.log("3 is best number")
    }
    console.log(element)
    
}

// In js there is no out of bound exception 